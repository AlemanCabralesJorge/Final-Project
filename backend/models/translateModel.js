export default class Translation { //Se importa la clase
    constructor(originalText, translatedText) { //Constructor que recibe parámetros
      this.originalText = originalText; //Letra original
      this.translatedText = translatedText; //Letra traducida
    }
  }
  